<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.finalgym">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WebActivity"></activity>
        <activity android:name=".AboutActivity" />
        <activity
            android:name=".EditActivity"
            android:label="Edit Plans" />
        <activity
            android:name=".AllPlansActivity"
            android:label="All Plans" />
        <activity
            android:name=".TrainingActivity"
            android:label="Training" />
        <activity
            android:name=".AllTrainingsActivity"
            android:label="All Trainings" />
        <activity
            android:name=".MainActivity"
            android:label="Final Gym">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>